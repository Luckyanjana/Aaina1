@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@model List<UserLoginDto>
@{
    ViewData["Title"] = "Index";
}

<section class="content">

    <div class="box">
        <div class="box-body table-responsive">
            <div class="col-sm-12">
                <div class="dataTables_wrapper form-inline dt-bootstrap no-footer">
                    <div class="row">
                        <div class="col-sm-6">

                        </div><div class="col-sm-6">
                            <div class="site-datatable-button-container pull-right">
                                <a href="/superadmin/admin/Add" data-toggle="modal" data-backdrop="static" data-keyboard="false" data-target="#modal-add-edit-admin" class="btn btn-sm btn-primary mg-l-5"><i class="fa fa-plus"></i> Add New</a>
                            </div>

                        </div>
                    </div><div class="row">
                        <div class="col-sm-12">
                            <table class="table table-bordered table-hover dataTable no-footer">

                                <thead>
                                    <tr>
                                        <th>S.No</th>
                                        <th>Name</th>
                                        <th>Email</th>
                                        <th>Company</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model.Any())
                                    {
                                        int i = 1;
                                        foreach (var item in Model)
                                        {
                                            <tr>
                                                <td>
                                                    @i
                                                </td>
                                                <td>@item.Fname @item.Lname</td>
                                                <td>@item.Email</td>
                                                <td>@item.Company</td>
                                                <td>
                                                    <a href="/superadmin/admin/Edit/@item.Id" data-toggle="modal" data-backdrop="static" data-keyboard="false" data-target="#modal-add-edit-admin" class="btn btn-sm btn-success mg-l-5"><i class="fa fa-pencil"></i> Edit</a>
                                                    <a href="/superadmin/admin/Delete/@item.Id" data-toggle="modal" data-backdrop="static" data-keyboard="false" data-target="#modal-delete-admin" class="btn btn-sm btn-danger mg-l-5"><i class="fa fa-trash"></i> Delete</a>
                                                </td>
                                            </tr>
                                            i++;

                                        }
                                    }
                                    else
                                    {
                                        <tr>
                                            <td colspan="4">admin not found</td>
                                        </tr>
                                    }

                                </tbody>
                            </table>

                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
</section>

@Html.Partial("_Modal", new Modal { ID = "modal-add-edit-admin", AreaLabeledId = "modal-add-edit-admin-label", Size = ModalSize.Medium })
@Html.Partial("_Modal", new Modal { ID = "modal-delete-admin", AreaLabeledId = "modal-action-delete-admin-label", Size = ModalSize.Small })
@section scripts{

    <script src="~/js/Customejs/Admin/AddEdit.js" asp-append-version="true"></script>

}
